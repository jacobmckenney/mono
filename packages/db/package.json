{
    "name": "@ekklesia/db",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "build": "cargo build --manifest-path client/Cargo.toml",
        "db:migrate": "sea-orm-cli migrate",
        "db:generate": "sea-orm-cli generate entity --output-dir ./client/src/entities --with-copy-enums --with-serde both -u $DATABASE_URL "
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "dotenv-cli": "^7.4.2"
    }
}
